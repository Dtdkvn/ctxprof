# Launch status

Ctxprof's canonical public source repository is <https://github.com/Dtdkvn/ctxprof>; the npm package is [`ctxprof`](https://www.npmjs.com/package/ctxprof).

As of 2026-08-12, the public source, hosted CI, `ctxprof@0.1.0`, provenance attestation, `v0.1.0` Action tag, and [GitHub release](https://github.com/Dtdkvn/ctxprof/releases/tag/v0.1.0) are live and independently smoke-tested on Node 24. This checkout tracks `origin/main`. The one-time first-publish token and both GitHub secret copies have been revoked/removed. Future new-version publication now fails closed until the npm account completes 2FA and registers `Dtdkvn/ctxprof`, `release.yml`, environment `release` as a trusted publisher.
