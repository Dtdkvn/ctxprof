# Launch status

Ctxprof's expected canonical repository is <https://github.com/Dtdkvn/ctxprof> and its expected npm package name is `ctxprof`.

As of 2026-08-12, this local launch tree has no Git remote and has not been published. The repository metadata records the intended destination; configuring a remote, pushing, enabling the `release` environment and npm trusted publishing, and creating the first reviewed version tag remain explicit maintainer actions.
