import type { ProfileRun } from "../types.js";

export interface DashboardOptions {
  mode?: "static" | "store" | "proxy";
  title?: string;
  generatedAt?: string;
}

export function renderDashboard(
  runs: readonly ProfileRun[],
  options: DashboardOptions = {},
): string {
  const data = JSON.stringify(runs).replace(/</g, "\\u003c").replace(/-->/g, "--\\u003e");
  const mode = options.mode ?? "static";
  const live = mode !== "static";
  const title = escapeHtml(options.title ?? "Ctxprof report");
  const generatedAt = escapeHtml(options.generatedAt ?? new Date().toISOString());
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="color-scheme" content="dark light">
  <meta http-equiv="Content-Security-Policy" content="default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; img-src data:; connect-src ${live ? "'self'" : "'none'"}; base-uri 'none'; form-action 'none'">
  <title>${title}</title>
  <style>
    :root{--bg:#080b10;--panel:#10151d;--panel2:#151c26;--line:#273140;--text:#f1f5f9;--muted:#8e9bad;--accent:#8b5cf6;--mint:#2dd4bf;--amber:#f59e0b;--red:#fb7185;--blue:#60a5fa;--shadow:0 24px 70px rgba(0,0,0,.32);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}
    *{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 78% -20%,rgba(124,58,237,.16),transparent 40%),var(--bg);color:var(--text);min-height:100vh;font-size:14px}button,input,select{font:inherit}button{color:inherit}.shell{min-height:100vh;display:grid;grid-template-columns:300px 1fr}.sidebar{border-right:1px solid var(--line);background:rgba(10,14,20,.88);backdrop-filter:blur(18px);padding:22px 16px;position:sticky;top:0;height:100vh;overflow:auto}.brand{display:flex;align-items:center;gap:11px;padding:2px 8px 22px}.mark{width:34px;height:34px;border-radius:10px;display:grid;place-items:center;background:linear-gradient(145deg,#9f7aea,#6d28d9);box-shadow:0 8px 25px rgba(124,58,237,.35)}.brand b{font-size:18px;letter-spacing:-.03em}.brand small{display:block;color:var(--muted);font-size:10px;text-transform:uppercase;letter-spacing:.13em;margin-top:2px}.search{width:100%;background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:10px 12px;color:var(--text);outline:none}.search:focus{border-color:#7c3aed;box-shadow:0 0 0 3px rgba(124,58,237,.14)}.section-label{color:#667386;font-size:10px;text-transform:uppercase;letter-spacing:.14em;margin:20px 8px 8px}.run-list{display:grid;gap:6px}.run{width:100%;text-align:left;border:1px solid transparent;background:transparent;padding:11px;border-radius:11px;cursor:pointer}.run:hover{background:var(--panel)}.run.active{background:linear-gradient(135deg,rgba(124,58,237,.17),rgba(45,212,191,.05));border-color:rgba(139,92,246,.38)}.run-top{display:flex;justify-content:space-between;gap:8px}.run-label{font-weight:650;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.run-time,.run-meta{font-size:11px;color:var(--muted)}.run-meta{display:flex;gap:7px;margin-top:6px}.privacy{margin-top:22px;padding:12px;border:1px solid var(--line);border-radius:12px;background:rgba(16,21,29,.7);color:var(--muted);font-size:11px;line-height:1.5}.privacy strong{color:var(--mint)}.main{padding:26px 30px 50px;min-width:0}.topbar{display:flex;justify-content:space-between;align-items:flex-start;gap:24px;margin-bottom:24px}.eyebrow{color:#a78bfa;text-transform:uppercase;letter-spacing:.14em;font-size:10px;font-weight:700}.topbar h1{font-size:28px;letter-spacing:-.04em;margin:5px 0 5px}.subtitle{color:var(--muted);font-size:13px}.badges{display:flex;gap:8px;align-items:center}.badge{font-size:11px;color:var(--muted);border:1px solid var(--line);border-radius:999px;padding:7px 10px;background:var(--panel)}.badge.live::before{content:"";width:6px;height:6px;border-radius:50%;background:var(--mint);display:inline-block;margin-right:7px;box-shadow:0 0 0 4px rgba(45,212,191,.12)}.metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin-bottom:14px}.metric{background:linear-gradient(145deg,var(--panel),#0e131a);border:1px solid var(--line);border-radius:14px;padding:16px;box-shadow:var(--shadow)}.metric-label{font-size:10px;text-transform:uppercase;letter-spacing:.12em;color:var(--muted)}.metric-value{font-size:25px;font-weight:720;letter-spacing:-.045em;margin-top:8px}.metric-note{font-size:10px;color:#69768a;margin-top:4px}.grid{display:grid;grid-template-columns:minmax(0,1.65fr) minmax(290px,.75fr);gap:14px}.card{border:1px solid var(--line);background:linear-gradient(150deg,rgba(18,24,33,.96),rgba(12,17,24,.98));border-radius:15px;overflow:hidden;box-shadow:var(--shadow)}.card-head{display:flex;align-items:center;justify-content:space-between;padding:15px 17px;border-bottom:1px solid var(--line)}.card-title{font-weight:660}.card-kicker{font-size:11px;color:var(--muted);margin-top:2px}.legend{display:flex;gap:9px;flex-wrap:wrap;justify-content:flex-end}.legend i{width:7px;height:7px;border-radius:2px;display:inline-block;margin-right:4px}.legend span{font-size:9px;color:var(--muted)}.treemap{height:400px;position:relative;margin:12px;background:#080b10;border-radius:10px;overflow:hidden}.tile{position:absolute;border:2px solid #0b0f15;border-radius:7px;padding:10px;overflow:hidden;text-align:left;cursor:pointer;transition:filter .15s,transform .15s;min-width:0;background:var(--tile)}.tile:hover,.tile:focus{filter:brightness(1.17);z-index:4;outline:2px solid rgba(255,255,255,.32);outline-offset:-3px}.tile-name{font-weight:680;font-size:12px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.tile-value{font-size:11px;opacity:.75;margin-top:3px}.tile-share{position:absolute;right:8px;bottom:6px;font-size:9px;opacity:.55}.insights{padding:8px 13px 14px;display:grid;gap:7px;max-height:430px;overflow:auto}.warning{display:grid;grid-template-columns:28px 1fr;gap:9px;padding:10px;border-radius:10px;border:1px solid transparent;background:rgba(255,255,255,.018)}.warning:hover{border-color:var(--line)}.warn-icon{width:25px;height:25px;border-radius:7px;display:grid;place-items:center;font-size:12px;background:rgba(245,158,11,.12);color:var(--amber)}.warning.critical .warn-icon{background:rgba(251,113,133,.12);color:var(--red)}.warning.info .warn-icon{background:rgba(96,165,250,.1);color:var(--blue)}.warn-title{font-size:12px;font-weight:650}.warn-detail{font-size:10px;line-height:1.45;color:var(--muted);margin-top:3px}.empty-insight{padding:28px 12px;text-align:center;color:var(--muted)}.detail{margin-top:14px}.detail-body{padding:16px 18px;display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:18px}.detail-stat b{display:block;font-size:16px;margin-top:4px}.detail-stat span{color:var(--muted);font-size:10px;text-transform:uppercase;letter-spacing:.09em}.preview{grid-column:1/-1;border-top:1px solid var(--line);padding-top:14px;color:#b6c0ce;white-space:pre-wrap;word-break:break-word;font:11px/1.6 ui-monospace,SFMono-Regular,Consolas,monospace}.compare{margin-top:14px}.compare-controls{display:flex;gap:8px}.select{background:var(--panel2);color:var(--text);border:1px solid var(--line);border-radius:8px;padding:7px 9px;font-size:11px}.diff-grid{padding:16px;display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.diff{border:1px solid var(--line);border-radius:11px;padding:13px}.diff-name{color:var(--muted);font-size:10px;text-transform:uppercase;letter-spacing:.09em}.diff-val{font-size:19px;font-weight:680;margin-top:5px}.positive{color:var(--red)}.negative{color:var(--mint)}.empty{max-width:650px;margin:12vh auto;text-align:center;padding:48px;border:1px dashed #354154;border-radius:20px;background:rgba(16,21,29,.6)}.empty-mark{width:62px;height:62px;border-radius:18px;margin:0 auto 17px;display:grid;place-items:center;background:rgba(124,58,237,.14);color:#a78bfa;font-size:28px}.empty h2{font-size:24px;margin:0 0 9px}.empty p{color:var(--muted);line-height:1.65}.empty code{display:block;text-align:left;margin-top:20px;background:#070a0f;border:1px solid var(--line);padding:14px;border-radius:10px;color:#c4b5fd;overflow:auto}.footer{display:flex;justify-content:space-between;color:#596579;font-size:10px;margin-top:18px;padding:0 3px}@media(max-width:1000px){.shell{grid-template-columns:230px 1fr}.main{padding:22px 18px}.metrics{grid-template-columns:repeat(2,1fr)}.grid{grid-template-columns:1fr}.treemap{height:360px}}@media(max-width:700px){.shell{display:block}.sidebar{height:auto;position:relative;border-right:0;border-bottom:1px solid var(--line)}.run-list{grid-template-columns:repeat(auto-fit,minmax(170px,1fr))}.privacy,.section-label{display:none}.main{padding:18px 13px 36px}.topbar{display:block}.badges{margin-top:12px}.metrics{gap:8px}.metric{padding:13px}.metric-value{font-size:20px}.treemap{height:430px}.detail-body{grid-template-columns:repeat(2,1fr)}.diff-grid{grid-template-columns:1fr}}
  </style>
</head>
<body>
  <div class="shell">
    <aside class="sidebar">
      <div class="brand"><div class="mark"><svg width="18" height="18" viewBox="0 0 24 24" fill="none"><path d="M5 18V9m5 9V5m5 13v-7m4 7V3" stroke="white" stroke-width="2.2" stroke-linecap="round"/></svg></div><div><b>ctxprof</b><small>Context profiler</small></div></div>
      <input id="search" class="search" placeholder="Filter captures…" aria-label="Filter captures">
      <div class="section-label">Recent captures</div>
      <div id="run-list" class="run-list"></div>
      <div class="privacy"><strong>● Local-first</strong><br>Ordinary headers are never stored. Optional Ctxprof labels become bounded run metadata.</div>
    </aside>
    <main class="main" id="app"><div class="empty"><div class="empty-mark">⌁</div><h2>Loading captures</h2><p>Reading the local profile store…</p></div></main>
  </div>
  <script>
  (function(){
    "use strict";
    var INITIAL=${data};
    var MODE=${JSON.stringify(mode)};
    var LIVE=MODE!=="static";
    var LIVE_LABEL=MODE==="proxy"?"Proxy live":MODE==="store"?"Capture store live":"Static report";
    var GENERATED_AT=${JSON.stringify(generatedAt)};
    var runs=INITIAL.slice();
    var selectedId=null;
    var selectedComponentId=null;
    var query="";
    var colors={system:"#7c3aed",developer:"#a855f7",tools:"#0f9f8f",message:"#2563a8",tool_result:"#c7770a",other:"#536070"};
    var labels={system:"System",developer:"Developer",tools:"Tools",message:"Messages",tool_result:"Tool results",other:"Other"};
    function h(value){return String(value==null?"":value).replace(/[&<>\"']/g,function(c){return c==="&"?"&amp;":c==="<"?"&lt;":c===">"?"&gt;":c.charCodeAt(0)===34?"&quot;":"&#39;";});}
    function tokens(n){return Number(n||0).toLocaleString()+" tok";}
    function money(n){return n==null?"Unknown":"$"+Number(n).toFixed(n<.01?5:3);}
    function shortTime(value){var d=new Date(value);return isNaN(d.getTime())?"":d.toLocaleString([], {month:"short",day:"numeric",hour:"2-digit",minute:"2-digit"});}
    function pct(n){return Math.round((n||0)*100)+"%";}
    function filtered(){return runs.filter(function(r){var value=(r.label+" "+r.model+" "+r.promptVersion).toLowerCase();return value.indexOf(query)>=0;});}
    function current(){var list=filtered();return list.find(function(r){return r.id===selectedId;})||list[0]||null;}
    function layout(items,x,y,w,hh,out){if(!items.length)return out;if(items.length===1){out.push({item:items[0],x:x,y:y,w:w,h:hh});return out;}var sum=items.reduce(function(a,c){return a+c.allocatedInputTokens;},0);var half=sum/2,acc=0,cut=1;for(var i=0;i<items.length-1;i++){acc+=items[i].allocatedInputTokens;if(acc>=half){cut=i+1;break;}}var left=items.slice(0,cut),right=items.slice(cut);var leftSum=left.reduce(function(a,c){return a+c.allocatedInputTokens;},0);var ratio=sum?leftSum/sum:.5;if(w>=hh){layout(left,x,y,w*ratio,hh,out);layout(right,x+w*ratio,y,w*(1-ratio),hh,out);}else{layout(left,x,y,w,hh*ratio,out);layout(right,x,y+hh*ratio,w,hh*(1-ratio),out);}return out;}
    function renderList(){var list=filtered();document.getElementById("run-list").innerHTML=list.map(function(r){return '<button class="run '+(current()&&current().id===r.id?'active':'')+'" data-run="'+h(r.id)+'"><div class="run-top"><span class="run-label">'+h(r.label)+'</span><span class="run-time">'+h(shortTime(r.capturedAt))+'</span></div><div class="run-meta"><span>'+h(r.model)+'</span><span>·</span><span>'+h(tokens(r.totals.providerInputTokens==null?r.totals.estimatedInputTokens:r.totals.providerInputTokens))+'</span></div></button>';}).join("");Array.prototype.forEach.call(document.querySelectorAll("[data-run]"),function(el){el.onclick=function(){selectedId=el.getAttribute("data-run");selectedComponentId=null;render();};});}
    function empty(){return '<div class="empty"><div class="empty-mark">⌁</div><h2>No context captures yet</h2><p>Import a fixture or point your OpenAI-compatible client at the proxy. No API key is needed to explore the deterministic demo.</p><code>ctxprof demo\\nctxprof proxy --port 8787</code></div>';}
    function metric(label,value,note){return '<div class="metric"><div class="metric-label">'+h(label)+'</div><div class="metric-value">'+h(value)+'</div><div class="metric-note">'+h(note)+'</div></div>';}
    function render(){renderList();var run=current();if(!run){document.getElementById("app").innerHTML=empty();return;}selectedId=run.id;var total=run.totals.providerInputTokens==null?run.totals.estimatedInputTokens:run.totals.providerInputTokens;var critical=run.warnings.filter(function(w){return w.severity==="critical"||w.severity==="warning";}).length;var legend=Object.keys(labels).map(function(k){return '<span><i style="background:'+colors[k]+'"></i>'+h(labels[k])+'</span>';}).join("");var tiles=layout(run.components.slice().sort(function(a,b){return b.allocatedInputTokens-a.allocatedInputTokens;}),0,0,100,100,[]).map(function(box){var c=box.item;return '<button class="tile" data-component="'+h(c.id)+'" style="left:'+box.x+'%;top:'+box.y+'%;width:'+box.w+'%;height:'+box.h+'%;--tile:'+colors[c.kind]+'" title="'+h(c.label+' · '+tokens(c.allocatedInputTokens))+'"><div class="tile-name">'+h(c.label)+'</div><div class="tile-value">'+h(tokens(c.allocatedInputTokens))+'</div><div class="tile-share">'+h(pct(c.share))+'</div></button>';}).join("");var warningHtml=run.warnings.length?run.warnings.map(function(w){return '<div class="warning '+h(w.severity)+'"><div class="warn-icon">'+(w.severity==='critical'?'!':w.severity==='info'?'i':'↗')+'</div><div><div class="warn-title">'+h(w.title)+'</div><div class="warn-detail">'+h(w.detail)+'</div></div></div>';}).join(""):'<div class="empty-insight">No obvious context waste found in this capture.</div>';var component=run.components.find(function(c){return c.id===selectedComponentId;})||run.components[0];var detail=component?'<div class="detail card"><div class="card-head"><div><div class="card-title">'+h(component.label)+'</div><div class="card-kicker">Component detail · estimates are directional</div></div><span class="badge">'+h(labels[component.kind])+'</span></div><div class="detail-body"><div class="detail-stat"><span>Allocated</span><b>'+h(tokens(component.allocatedInputTokens))+'</b></div><div class="detail-stat"><span>Raw estimate</span><b>'+h(tokens(component.estimatedTokens))+'</b></div><div class="detail-stat"><span>Input share</span><b>'+h(pct(component.share))+'</b></div><div class="detail-stat"><span>Cost share</span><b>'+h(money(component.estimatedCostUsd))+'</b></div><div class="detail-stat"><span>Content hash</span><b>'+h(component.contentHash)+'</b></div><div class="preview">'+h(component.preview||"Preview disabled")+'</div></div></div>':'';var versions=Array.from(new Set(runs.map(function(r){return r.promptVersion;}))).sort();var opts=versions.map(function(v){return '<option value="'+h(v)+'">'+h(v)+'</option>';}).join("");document.getElementById("app").innerHTML='<div class="topbar"><div><div class="eyebrow">Context capture</div><h1>'+h(run.label)+'</h1><div class="subtitle">'+h(run.model)+' · '+h(run.endpoint)+' · '+h(run.promptVersion)+'</div></div><div class="badges"><span class="badge '+(LIVE?'live':'')+'">'+h(LIVE_LABEL)+'</span><span class="badge">'+h(run.tokenizer.exact?'Exact tokens':'Estimated breakdown')+'</span></div></div><section class="metrics">'+metric("Input context",tokens(total),run.totals.providerInputTokens==null?'Component estimate':'Provider-reported total')+metric("Estimated cost",money(run.totals.estimatedTotalCostUsd),run.pricing?'Standard text rates · '+run.pricing.checkedAt:'Add a pricing catalog')+metric("Potential waste",tokens(run.totals.estimatedWasteTokens),'Heuristic, validate before removal')+metric("Actionable warnings",String(critical),critical?'Review before shipping':'Looking lean')+'</section><div class="grid"><section class="card"><div class="card-head"><div><div class="card-title">Context treemap</div><div class="card-kicker">Area = allocated input tokens</div></div><div class="legend">'+legend+'</div></div><div class="treemap">'+tiles+'</div></section><section class="card"><div class="card-head"><div><div class="card-title">Waste signals</div><div class="card-kicker">Explainable heuristics, never silent deletion</div></div><span class="badge">'+run.warnings.length+'</span></div><div class="insights">'+warningHtml+'</div></section></div>'+detail+compareCard(versions,opts)+'<div class="footer"><span>Generated '+h(GENERATED_AT)+'</span><span>Ctxprof · data stays on this machine</span></div>';Array.prototype.forEach.call(document.querySelectorAll("[data-component]"),function(el){el.onclick=function(){selectedComponentId=el.getAttribute("data-component");render();};});bindCompare(versions);renderList();}
    function versionSummary(version){var selected=runs.filter(function(r){return r.promptVersion===version;});if(!selected.length)return null;var priced=selected.every(function(r){return r.totals.estimatedTotalCostUsd!=null;});return{count:selected.length,input:selected.reduce(function(a,r){return a+(r.totals.providerInputTokens==null?r.totals.estimatedInputTokens:r.totals.providerInputTokens);},0)/selected.length,total:selected.reduce(function(a,r){return a+r.totals.totalTokens;},0)/selected.length,cost:priced?selected.reduce(function(a,r){return a+r.totals.estimatedTotalCostUsd;},0)/selected.length:null};}
    function compareCard(versions,opts){if(versions.length<2)return '<section class="compare card"><div class="card-head"><div><div class="card-title">Prompt-version diff</div><div class="card-kicker">Import captures tagged with two versions to unlock A/B comparison.</div></div></div></section>';return '<section class="compare card"><div class="card-head"><div><div class="card-title">Prompt-version diff</div><div class="card-kicker">Averages across captures with the same version</div></div><div class="compare-controls"><select id="from-version" class="select">'+opts+'</select><span>→</span><select id="to-version" class="select">'+opts+'</select></div></div><div id="diff-grid" class="diff-grid"></div></section>';}
    function bindCompare(versions){if(versions.length<2)return;var from=document.getElementById("from-version"),to=document.getElementById("to-version");from.value=versions[0];to.value=versions[versions.length-1];function update(){var a=versionSummary(from.value),b=versionSummary(to.value);if(!a||!b)return;var delta=b.input-a.input,cost=a.cost==null||b.cost==null?null:b.cost-a.cost;document.getElementById("diff-grid").innerHTML=diff("Input tokens",(delta>=0?"+":"")+Math.round(delta).toLocaleString(),delta)+diff("Total tokens",((b.total-a.total)>=0?"+":"")+Math.round(b.total-a.total).toLocaleString(),b.total-a.total)+diff("Estimated cost",cost==null?"Unknown":((cost>=0?"+":"")+money(cost)),cost);}
      from.onchange=update;to.onchange=update;update();}
    function diff(name,value,delta){var cls=delta==null?"":delta>0?"positive":delta<0?"negative":"";return '<div class="diff"><div class="diff-name">'+h(name)+'</div><div class="diff-val '+cls+'">'+h(value)+'</div></div>';}
    document.getElementById("search").addEventListener("input",function(e){query=String(e.target.value||"").toLowerCase();selectedId=null;render();});
    var pollTimer=null,polling=false,pollDelay=2000;
    function schedulePoll(){if(!LIVE||document.hidden||pollTimer!==null)return;pollTimer=setTimeout(function(){pollTimer=null;pollRuns();},pollDelay);}
    function pollRuns(){if(!LIVE||document.hidden||polling){schedulePoll();return;}polling=true;fetch("/api/runs?limit=1000").then(function(response){if(!response.ok)throw new Error("HTTP "+response.status);return response.json();}).then(function(data){runs=Array.isArray(data.runs)?data.runs:[];pollDelay=2000;render();}).catch(function(error){pollDelay=Math.min(30000,pollDelay*2);if(!runs.length)document.getElementById("app").innerHTML='<div class="empty"><div class="empty-mark">!</div><h2>Could not load captures</h2><p>'+h(error.message)+'</p><p>Ctxprof will retry automatically.</p></div>';}).then(function(){polling=false;schedulePoll();});}
    document.addEventListener("visibilitychange",function(){if(document.hidden){if(pollTimer!==null){clearTimeout(pollTimer);pollTimer=null;}}else{pollRuns();}});
    function boot(){render();if(LIVE)pollRuns();}
    boot();
  })();
  </script>
</body>
</html>`;
}

function escapeHtml(value: string): string {
  return value.replace(/[&<>"']/g, (character) => {
    const entities: Record<string, string> = {
      "&": "&amp;",
      "<": "&lt;",
      ">": "&gt;",
      '"': "&quot;",
      "'": "&#39;",
    };
    return entities[character] ?? character;
  });
}
